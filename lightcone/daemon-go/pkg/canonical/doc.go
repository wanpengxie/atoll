// Package canonical will host the RFC 8785 canonical JSON + SHA-256
// hashing helpers shared between daemon and worker, introduced by
// ticket T2 (.dalek/pm/m1.3-tickets.md §T2).
package canonical
