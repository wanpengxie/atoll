# Chrome Extension Configuration

# Chrome Extension ID (get this after loading the unpacked extension)
CHROME_EXTENSION_ID=your-extension-id-here

# Development mode
NODE_ENV=development

# Coagent daemon HTTP base URL (WS shares the same host, upgrades via /device/{id})
COAGENT_DAEMON_URL=http://127.0.0.1:9501
