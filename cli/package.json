{
  "name": "@coagent/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "coagent": "./bin/coagent",
    "xhs": "./bin/xhs",
    "coagent-kernel": "./bin/coagent-kernel",
    "coagent-msg": "./bin/coagent-msg"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "pnpm build && node --test test/*.test.mjs",
    "lint": "pnpm build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@coagent/payload-types": "workspace:*",
    "commander": "^14.0.3",
    "ulid": "^3.0.2",
    "yaml": "^2.8.3"
  }
}
