// Package contract declares the proxy daemon v2 wire constants and
// serializable frame shapes.
//
// The package intentionally contains no validation, storage, routing, or
// network code. Server and daemon implementations should import these types
// as shared contract references while owning their own behavior.
package contract
