// Package v4types will host the v4 Envelope struct, kind enums, and the
// three-class reason enum (HarnessRejectReason / InstallReason /
// TerminalFailureReason) shared between daemon and worker, introduced
// by ticket T2 (.dalek/pm/m1.3-tickets.md §T2).
package v4types
