// Package actorhost owns desired-to-actual convergence for one actor execution
// domain. It manages exact actorrt Units and remote Bindings; it does not own
// logical actor identity, authorization, or wire transport.
package actorhost
